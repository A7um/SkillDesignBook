# AGENTS.md

## Cursor Cloud specific instructions

This is a documentation-only repository (`SkillDesignBook`) containing Markdown chapters about how to write SKILL.md files for AI agents.

- **No build/lint/test/run commands** — pure Markdown content, no code, no dependencies, no services.
- **No package manager or dependency manifests** — nothing to install.
- Content lives in `book/` (chapters 01-05 + source catalog) and `README.md`.
