# AGENTS.md

## Cursor Cloud specific instructions

This is a newly initialized repository (`SkillDesignBook`) with no application code, dependencies, or services yet. The repository contains only a `README.md`.

- **No build/lint/test commands exist** — there are no `package.json`, `Makefile`, or other build configuration files.
- **No services to start** — there are no backend, frontend, or database services defined.
- **No dependencies to install** — there are no dependency manifests (`package.json`, `requirements.txt`, etc.).

Once application code and dependency files are added, update this section with:
- How to install dependencies
- How to run lint, tests, and build
- How to start development services
- Any non-obvious caveats discovered during setup
